services:
  - type: web
    name: chatbot
    env: python
    region: oregon     # or your preferred region
    plan: free         # or starter, whatever you chose
    pythonVersion: "3.12.2"
    buildCommand: "pip install -r requirements.txt"
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT